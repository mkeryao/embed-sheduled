# 轻量级任务调度系统
内嵌式轻量级的基于Spring Task的任务调度系统，可以方便地集成到现有Spring Boot应用中，提供灵活的定时任务管理和执行能力。

## 功能特点

### 基础功能

- 基于Spring原生调度能力，无需第三方调度框架
- 支持通过数据库持久化任务配置
- 支持任务分组管理
- 支持Cron表达式配置执行周期
- 支持任务失败重试机制
- 支持多种通知方式（日志、企业微信等）
- 支持任务执行历史记录
- 支持排除特定日期和时间段执行
- 支持UI界面对任务进行操作

### 新增功能

1. **简单任务流支持**
   - 支持定义多节点任务流程
   - 支持节点之间数据传递
   - 支持条件节点和表达式评估
   - 支持全局参数和节点参数
   - 支持失败策略配置（继续执行或停止）
   - 支持任务流执行历史记录

2. **分布式锁优化**
   - 增强的分布式锁管理器
   - 支持锁获取重试机制
   - 更灵活的锁配置和管理
   - 活跃锁缓存，防止锁过早释放
   - 锁状态跟踪和日志记录

### 系统要求

- 轻量级 使用数据库操作JdbcTemplate 不要使用JPA等重量级框架
- 轻量级 不要使用 Spring Security等重量级框架 建议可以使用Jwt简单身份认证
- 轻量级 不要使用 themlef等模板引擎
- 采用JDK1.8 
- UI界面要求简单 使用Html+Jquery+CSS+JS+Boostrap 等，不要使用VUE或者React等需要编译的前端框架
- 基于给定的表结构开发，不要无故的扩展
- 代码结构清晰 便于维护